import $ from "jquery";

import "bootstrap";

window.$ = window.jQuery = $;
import "alpinejs";

import scrollToReveal from "./components/header";

window.scrollToReveal = scrollToReveal;

$(() => {});
